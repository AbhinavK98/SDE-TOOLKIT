# Bit Manipulation Practice (Foundations)

1. Check if number is even (n & 1 == 0)
2. Count set bits
3. Single Number (XOR)
4. Power of Two
5. Generate all subsets using bitmask

Each: brute → bit trick → explain WHY XOR works.
