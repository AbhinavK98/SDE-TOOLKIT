# Modular Arithmetic

## WHY mod

Prevent overflow; keep numbers bounded in combinatorics.

## Rules

(a + b) % m = ((a % m) + (b % m)) % m  
(a * b) % m = ((a % m) * (b % m)) % m

## WHEN

Large factorials, counting paths, hash mappings.
